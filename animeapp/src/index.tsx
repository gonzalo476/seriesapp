import React from 'react'
import { View, SafeAreaView } from 'react-native'

import { Text } from '../constants'

const index = () => {
    return (
        <View>
            <SafeAreaView />
            <Text variant="largeTitle">SERIES APP SRC</Text>
        </View>
    )
}

export default index
